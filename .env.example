# EVA Nepal — Environment Variables
# Copy to .env.local for local development
# Copy to .env.production for server deployment

# App URL (no trailing slash)
NEXT_PUBLIC_SITE_URL=https://evanepal.org

# Port (used by PM2 ecosystem.config.js)
PORT=3011

# Node environment
NODE_ENV=production
